/**********************************************
string.gs
Stringオブジェクトを操作する汎用的な機能を保持
**********************************************/

/*
機能:
    引数のstringから、空白を削除して返却する
引数:
    str: 文字列(String)
*/
function deleteSpace(str) {
  return str.replace(/[\s\t\n]/g, "");
}
